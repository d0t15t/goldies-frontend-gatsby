import React, { FC, ReactNode } from 'react';
import * as S from './{{name}}.styled';

interface {{name}}Props {
  children: ReactNode;
}

export const {{name}}: FC<{{name}}Props> = ({children}) => {
  return <S.Container>{{name}}{children}</S.Container>;
};

export default {{name}};